<%= form_with(model: dy, local: true) do |form| %>
  <% if dy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dy.errors.count, "error") %> prohibited this dy from being saved:</h2>

      <ul>
        <% dy.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fk_user_id %>
    <%= form.number_field :fk_user_id %>
  </div>

  <div class="field">
    <%= form.label :colour %>
    <%= form.text_field :colour %>
  </div>

  <div class="field">
    <%= form.label :sides %>
    <%= form.number_field :sides %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
